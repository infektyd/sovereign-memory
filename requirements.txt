# Core
sentence-transformers>=2.2.0
numpy>=1.24.0

# FAISS (CPU — use faiss-gpu if you have CUDA)
faiss-cpu>=1.7.4

# Cross-encoder re-ranking (included in sentence-transformers)
# No extra install needed

# Token counting for context budgeting
tiktoken>=0.5.0

# File watcher
watchdog>=3.0.0

# Optional: for better SQLite FTS tokenization
# icu4c (system package)
