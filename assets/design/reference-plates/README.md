# Sovereign Memory Reference Plates

These images are synthetic visual references for the Sovereign Memory Console design system. They are not product screenshots, not generated from private vault data, and not normative.

The root [`DESIGN.md`](../../../DESIGN.md) is the source of truth. Use these plates to preserve the intended feel: warm paper, graphite control-room structure, dense evidence panels, mono machine labels, small radii, hairline borders, and clear privacy/AFM/learning states.

## Plates

- `console-overview.png` - Three-pane operational console with local status, ranked evidence, dry-run summary, and right-side source inspector.
- `source-inspector.png` - Source detail view with metadata, excerpt, risk annotations, JSON packet preview, and recommended handling.
- `dry-run-review.png` - Dry-run learning review split into learn candidates, log-only items, and do-not-store items.
- `component-state-matrix.png` - Core components across default, hover, focus, selected, private, warning, danger, do-not-store, log-only, learn, and error states.

## Prompt Intent

If these are regenerated with an image model, use synthetic data only. The visual prompt should ask for a dense local memory console, warm paper background, graphite navigation, verdigris primary actions, persimmon danger, mustard warnings, blue information states, IBM Plex Mono-style evidence labels, small radii, hairline borders, visible focus states, no glass, no gradients, no decorative blobs, and no private or real file paths.

Do not include raw session content, local database names, adapter paths, launchd plists, private datasets, vault raw/log text, secrets, or real personal paths.
